<UserControl 
    x:Class="SpriteFactory.GameObjects.GameObjectView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d" 
    d:DesignHeight="400" 
    d:DesignWidth="400"
    d:DataContext="{d:DesignInstance local:GameObjectsViewModel, IsDesignTimeCreatable=False}">
    <ListBox 
        Name="ListBox"
        ItemsSource="{Binding Items}" 
        SelectedItem="{Binding SelectedItem}"
        MouseMove="ListBox_MouseMove"
        MouseLeave="ListBox_OnMouseLeave"
        AllowDrop="True">
        <ListBox.ItemTemplate>
            <DataTemplate DataType="local:GameObject">
                <DockPanel LastChildFill="True">
                    <Image 
                        DockPanel.Dock="Left"
                        Source="{Binding FullPath}" 
                        Stretch="Uniform" 
                        StretchDirection="DownOnly" 
                        Width="64" 
                        Height="64" />
                    <TextBlock 
                        VerticalAlignment="Center"
                        Text="{Binding Name}" />
                </DockPanel>
            </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>
</UserControl>
